export type Credentials = {
    accountId: string;
    applicationKey: string;
    apiUrl: string;
    authorizationToken: string;
    downloadUrl: string;
    recommendedPartSize: string;
    bucketName: string;
    bucketId: string;
}